name: Bundle Analysis

on:
  pull_request:
    branches: [master, main]
    paths:
      - "src/**"
      - "package.json"
      - "bun.lock"
      - "next.config.ts"

permissions:
  contents: read
  pull-requests: write

jobs:
  analyze:
    name: Analyze Bundle Size
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build with Bundle Analysis
        run: bun run build
        env:
          ANALYZE: true
          NEXT_PUBLIC_BACKEND_API_URL: ${{ secrets.NEXT_PUBLIC_BACKEND_API_URL }}
          NEXT_PUBLIC_FRONTEND_URL: ${{ secrets.NEXT_PUBLIC_FRONTEND_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}

      - name: Report Bundle Size
        run: |
          echo "## 📦 Bundle Size Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -d ".next/analyze" ]; then
            echo "| Chunk | Size |" >> $GITHUB_STEP_SUMMARY
            echo "|-------|------|" >> $GITHUB_STEP_SUMMARY
            find .next/analyze -name "*.html" -exec echo "| {} | check report |" >> $GITHUB_STEP_SUMMARY \;
          else
            echo "Bundle analysis not available (install @next/bundle-analyzer)" >> $GITHUB_STEP_SUMMARY
          fi
