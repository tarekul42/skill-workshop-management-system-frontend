// Test file for self-healing CI/CD pipeline validation
// Intentionally contains style violations for auto-fix testing

const unusedVariable = 'This variable is never used'

const message = 'Hello, world!'

const PI = 3.14159

function greet(name: string) {
  const greeting = "Hi, " + name
  return greeting
}

export { greet }
